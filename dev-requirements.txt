pytest
pytest-django
pytest-cov
# note: pytest-sugar is optional but pretty
pytest-sugar
django-debug-toolbar
sphinx
# Optional, only needed if deploying
ansible
# pytest-django-haystack
# local fork adds haystack connection parameter
git+https://github.com/Princeton-CDH/pytest-django-haystack.git@haystack-parameter#egg=pytest-django-haystack

